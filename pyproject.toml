[project]
name = "mcp-weather"
version = "0.1.0"
description = "Weather forecast MCP server using QWeather API"
requires-python = ">=3.13"
dependencies = ["fastmcp>=3.0.0", "httpx", "cryptography", "pydantic-settings", "python-dotenv"]

[dependency-groups]