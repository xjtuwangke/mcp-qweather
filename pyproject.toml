[project]
name = "mcp-weather"
version = "0.1.0"
description = "Weather forecast MCP server using QWeather API"
requires-python = ">=3.13"
dependencies = ["fastmcp", "httpx", "PyJWT", "cryptography", "pydantic-settings", "python-dotenv"]

[dependency-groups]